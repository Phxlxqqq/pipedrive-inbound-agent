{
  "name": "pipedrive-inbound-agent",
  "version": "1.0.0",
  "private": true,
  "description": "Serverless Webhook-Handler für Pipedrive (Vercel Functions)",
  "license": "MIT",
  "engines": {
    "node": "22.x"
  },
  "scripts": {
    "vercel-build": "echo \"no build step for api functions\"",
    "build": "echo \"nothing to build\"",
    "start": "echo \"use Vercel to run serverless function\""
  },
  "devDependencies": {
    "@types/node": "^24.10.0",
    "typescript": "^5.6.0"
  },
  "dependencies": {
    "openai": "^6.8.1"
  }
}
